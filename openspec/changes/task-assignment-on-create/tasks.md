# Tasks

## 1. Failing E2E spec

- [x] 1.1 Add `data-testid` attributes needed for a stable e2e test (none of these components have any today): the "Tasks" page's "+ Add" trigger button (`apps/gauzy/src/app/pages/tasks/components/task/task.component.html:168-175`), the add-task dialog's title input and Save button (`packages/ui-core/shared/src/lib/tasks/add-task-dialog/add-task-dialog.component.html`, title input ~line 118-124, Save button ~line 232-234), and the rendered member chip in the task list row (`packages/ui-core/shared/src/lib/table-components/people-list/people-list.component.html`, named-person anchor ~line 9 and stacked-avatar anchor ~line 23), e.g. `[attr.data-testid]="'task-member-' + person.id"` keyed off `IPersonListItem.id`.
- [x] 1.2 Write `apps/poc-e2e/tests/task-creation-self-assign.spec.ts`: log in as the seeded Employee user (`employee@ever.co` / `12345678`, role `EMPLOYEE`), navigate to the "Tasks" page, open the add-task dialog via the new trigger test id, fill in only the title (no members selected), save, and assert the created task's row shows a member chip for the logged-in employee's id via the new `data-testid`. This is red today: `AddTaskDialogComponent` defaults `members: []` and `TaskCreateHandler` never adds the creator (see `specs/task-assignment/spec.md` scenario "Employee creates a task without selecting any members").

## 2. Backend auto-assignment

- [x] 2.1 In `TaskCreateHandler.execute()` (`packages/core/src/lib/tasks/commands/handlers/task-create.handler.ts:54,83-90`), read `RequestContext.currentEmployeeId()` and, when it resolves to a non-null id not already present in `members`, add it to `members` before the `members.map(({ id }) => new Employee({ id }))` call at line 85 — no change when it resolves to `null` (Admin/Manager acting for someone else, or no employee in context at all). Verified by the spec from task 1 passing.
- [x] 2.2 Add Jest unit tests for `TaskCreateHandler.execute()` (new `task-create.handler.spec.ts` alongside the handler) covering: an Employee creator with an empty `members` list gets self-added; an Employee creator with other explicit members gets appended (not replaced); an Employee creator who already included themselves is not duplicated; a caller with no resolvable current employee id (simulating both Admin/Manager and an unauthenticated/integration-sync context) leaves `members` exactly as supplied, including empty.
